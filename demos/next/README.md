```sh
# Development
npx lerna run --scope demo-next --stream dev

# Build for production
npx lerna run --scope demo-next --stream build

# Start production server
npx lerna run --scope demo-next --stream start
```